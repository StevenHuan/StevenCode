using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace 映射
{
    class Program
    {
        static void Main(string[] args)
        {
            //C#编译后的文件主要是由IL代码和元数据组成，元数据为.NET组件提供了丰富的自描述特性，它使得我们可以在吗运行时获取组件中的类型等重要信息，C#中这是通过映射(reflection)机制来完成的
            
        }
    }
}
